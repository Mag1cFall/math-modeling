解决 C 题，使用数学建模技能
ULTRAWORK
所有你的产物放到 D:\杂大三下\CUMCM2025Problems\C题\work\ 下面，
每一问对应一个独立的 py 文件（q1.py、q2.py、q3.py ...），
代码只负责计算、出图、输出结果，不得硬编码任何结论 / 判断 / 预设值；
所有输出（图片、数据、日志）单独放一个 output 文件夹，结束后清理临时文件。
有缺少的环境就补齐，用 anaconda python 环境即可。

工作方式：先完整读 skill 的 SKILL.md、理解所有关键原则；
按阶段零先向我索要比赛规则文档（若未提供则明示沿用默认并声明）；
读题后按题型选读 skill 里相关的算法文档、latex-guide、角色说明等；
建立详细 todo，按阶段零→一→二→三→四顺序完整执行，直到交付。

最终交付：LaTeX 项目编译出的 paper/main.pdf + 可复现的 q1.py ~ qN.py + output/ 目录下分层组织的计算结果、日志、图表。
